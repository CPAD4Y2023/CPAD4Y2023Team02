apiVersion: v1
kind: Service
metadata:
  name: recylone-catalog-service
  namespace: recyclone-dev
spec:
  selector:
    app: recylone-catalog-service
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080