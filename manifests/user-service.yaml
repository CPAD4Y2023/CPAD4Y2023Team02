apiVersion: v1
kind: Service
metadata:
  name: recylone-user-service
  namespace: recyclone-dev
spec:
  selector:
    app: recylone-user-service
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000